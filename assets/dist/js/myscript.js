/*-- Flash Data Department --*/
const flash_data_department = $('.flash-data-department').data('flashdata');

if (flash_data_department) {
	Swal.fire(
	  'Department Data!',
	   flash_data_department + ' Successfully',
	  'success'
	)
}

/*-- Flash Data Division--*/
const flash_data_division = $('.flash-data-division').data('flashdata');

if (flash_data_division) {
	Swal.fire(
	  'Division Data!',
	   flash_data_division + ' Successfully',
	  'success'
	)
}

/*-- Flash Data position--*/
const flash_data_position = $('.flash-data-position').data('flashdata');

if (flash_data_position) {
	Swal.fire(
	  'Position Data!',
	   flash_data_position + ' Successfully',
	  'success'
	)
}

/*-- Flash Data Employee --*/
const flash_data_employee = $('.flash-data-employee').data('flashdata');

if (flash_data_employee) {
	Swal.fire(
	  'Employee Data!',
	   flash_data_employee + ' Successfully',
	  'success'
	)
}

/*-- Flash Data User --*/
const flash_data_user = $('.flash-data-user').data('flashdata');

if (flash_data_user) {
	Swal.fire(
		'User Data!',
	   	flash_data_user + ' Successfully',
	  	'success'
	 )
}


/*-- Flash Data Career --*/
const flash_data_career = $('.flash-data-career').data('flashdata');

if (flash_data_career) {
	Swal.fire(
		'Career Data!',
	   	flash_data_career + ' Successfully',
	  	'success'
	 )
}

/*-- Flash Data Education --*/
const flash_data_education = $('.flash-data-education').data('flashdata');

if (flash_data_education) {
	Swal.fire(
		'Education Data!',
	   	flash_data_education + ' Successfully',
	  	'success'
	 )
}

/*-- Flash Data Family --*/
const flash_data_family = $('.flash-data-family').data('flashdata');

if (flash_data_family) {
	Swal.fire(
		'Family Data!',
	   	flash_data_family + ' Successfully',
	  	'success'
	 )
}

/*-- Flash Data Family --*/
const flash_data_bpjs = $('.flash-data-bpjs').data('flashdata');

if (flash_data_bpjs) {
	Swal.fire(
		'BPJS Data!',
	   	flash_data_bpjs + ' Successfully',
	  	'success'
	 )
}

/*-- Flash Data Leave --*/
const flash_data_leave = $('.flash-data-leave').data('flashdata');

if (flash_data_leave) {
	Swal.fire(
		'Annual Leave Data!',
	   	flash_data_leave + ' Successfully',
	  	'success'
	 )
}

/*-- Flash Data Mass Leave --*/
const flash_data_mass_leave = $('.flash-data-mass-leave').data('flashdata');

if (flash_data_mass_leave) {
	Swal.fire(
		'Mass Leave Data!',
	   	flash_data_mass_leave + ' Successfully',
	  	'success'
	 )
}


